[workspace]
resolver = "3"
members = ["crates/*"]
exclude = ["crates/epaint_default_fonts"]
default-members = ["crates/paiagram"]

[workspace.package]
version = "0.1.3"
edition = "2024"
description = "Web/desktop timetabling application"
documentation = "https://paiagram.com/docs"
readme = "README.md"
homepage = "https://paiagram.com"
repository = "https://github.com/wensimehrp/paiagram"
license = "MPL-2.0"
keywords = ["diagram", "scheduler", "timetabling", "web"]
categories = ["visualization"]

[workspace.dependencies]
async-executor = "1.14.0"
# WGSL data communication
bytemuck = { version = "1.23.2", features = ["extern_crate_alloc"] }
# CLI
clap = { version = "4.5.51", features = ["derive"] }
# Interesting string
ecow = { version = "0.3.0", features = ["serde"] }
# egui stuff
eframe = {
  version = "0.36.1",
  features = [
    "default_fonts",
    "accesskit",
    "links",
    "persistence",
    "wayland",
    "web_screen_reader",
    "wgpu",
  ]
}
egui = {
  version = "0.36.1",
  features = [
    "persistence",
    "rayon",
    "serde",
  ]
}
egui_extras = { version = "0.36.1", features = ["webp"] }
# Internationalization
egui-i18n = { version = "0.2.0", features = ["fluent"] }
egui_tiles = "0.17.1"
either = "1.15.0"
encoding_rs = "0.8.35"
env_logger = "0.11.11"
eros = "0.5.0"
fontdb = "0.24.0"
futures-lite = "2.6.1"
git-version = "0.3.9"
ib-matcher = { version = "0.4.3", features = ["pinyin", "romaji"] }
indexmap = "2.14.0"
itertools = "0.14.0"
itoa = "1.0.18"
jiff = "0.2.29"
js-sys = "0.3"
log = "0.4.33"
lz4_flex = "0.12.0"
num-format = "0.4.4"
paste = "1.0.15"
petgraph = { version = "0.8.3", features = ["rayon", "serde-1"] }
rayon = "1.12.0"
rfd = "0.17.2"
rhai = "1.25.1"
ron = "0.12.0"
rustc-hash = "2.1.2"
serde = { version = "1.0.228", features = ["derive", "rc"] }
serde_json = "1.0.145"
slotmap = { version = "1.1.1", features = ["serde"] }
smallvec = { version = "1.15.1", features = ["serde"] }
thiserror = "2.0.18"
urlencoding = "2.1.3"
vec1 = "1.12.1"
wasm-bindgen = "0.2.122"
wasm-bindgen-futures = "0.4"
web-time = "1.1.0"

[patch.crates-io]
epaint_default_fonts = { path = "crates/epaint_default_fonts" }

# Enable a small amount of optimization in the dev profile.
[profile.dev]
opt-level = 1

# Enable a large amount of optimization in the dev profile for dependencies.
[profile.dev.package."*"]
opt-level = 2

# Enable more optimization in the release profile at the cost of compile time.
[profile.release]
opt-level = 2
strip = true
lto = "fat"
panic = "abort"
codegen-units = 1

# Profile for converter. For some reason the converter requires strip to be debuginfo
[profile.converter]
inherits = "wasm-release"
strip = "debuginfo"

# Optimize for size in the wasm-release profile to reduce load times and bandwidth usage on web.
[profile.wasm-release]
inherits = "release"
opt-level = "s"
